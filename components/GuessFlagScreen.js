import { useState } from "react";
import { View, Text, TouchableOpacity, StyleSheet } from "react-native";
const GuessFlagScreen = () => {
  return (
    <View>
      <Text>It is the GuessFlagScreen</Text>
    </View>
  );
};
export default GuessFlagScreen;
